# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class RestaurantActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Restaurant]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Restaurant.prisma().query_raw(
            'SELECT * FROM Restaurant WHERE id = ?',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Restaurant
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Restaurant.prisma().query_first(
            'SELECT * FROM Restaurant WHERE name = ?',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.RestaurantCreateInput,
        include: Optional[types.RestaurantInclude] = None
    ) -> _PrismaModelT:
        """Create a new Restaurant record.

        Parameters
        ----------
        data
            Restaurant record data
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The created Restaurant record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Restaurant record from just the required fields
        restaurant = await Restaurant.prisma().create(
            data={
                # data to create a Restaurant record
                'name': 'ggciceaie',
                'location': 'bbehjachib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.RestaurantCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Restaurant records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Restaurant record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Restaurant.prisma().create_many(
            data=[
                {
                    # data to create a Restaurant record
                    'name': 'cadfabfehe',
                    'location': 'dgiiaaijj',
                },
                {
                    # data to create a Restaurant record
                    'name': 'bfaiacjjfc',
                    'location': 'eigcfgbif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.RestaurantWhereUniqueInput,
        include: Optional[types.RestaurantInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Restaurant record.

        Parameters
        ----------
        where
            Restaurant filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The deleted Restaurant record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        restaurant = await Restaurant.prisma().delete(
            where={
                'id': 1062517886,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.RestaurantWhereUniqueInput,
        include: Optional[types.RestaurantInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Restaurant record.

        Parameters
        ----------
        where
            Restaurant filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The found Restaurant record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        restaurant = await Restaurant.prisma().find_unique(
            where={
                'id': 267834847,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.RestaurantWhereUniqueInput,
        include: Optional[types.RestaurantInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Restaurant record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Restaurant filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The found Restaurant record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        restaurant = await Restaurant.prisma().find_unique_or_raise(
            where={
                'id': 180171308,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
        include: Optional[types.RestaurantInclude] = None,
        order: Optional[Union[types.RestaurantOrderByInput, List[types.RestaurantOrderByInput]]] = None,
        distinct: Optional[List[types.RestaurantScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Restaurant records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Restaurant records returned
        skip
            Ignore the first N results
        where
            Restaurant filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Restaurant model
        order
            Order the returned Restaurant records by any field
        distinct
            Filter Restaurant records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Restaurant]
            The list of all Restaurant records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Restaurant records
        restaurants = await Restaurant.prisma().find_many(take=10)

        # find the first 5 Restaurant records ordered by the location field
        restaurants = await Restaurant.prisma().find_many(
            take=5,
            order={
                'location': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
        include: Optional[types.RestaurantInclude] = None,
        order: Optional[Union[types.RestaurantOrderByInput, List[types.RestaurantOrderByInput]]] = None,
        distinct: Optional[List[types.RestaurantScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Restaurant record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Restaurant filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Restaurant model
        order
            Order the returned Restaurant records by any field
        distinct
            Filter Restaurant records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Restaurant
            The first Restaurant record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Restaurant record ordered by the id field
        restaurant = await Restaurant.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
        include: Optional[types.RestaurantInclude] = None,
        order: Optional[Union[types.RestaurantOrderByInput, List[types.RestaurantOrderByInput]]] = None,
        distinct: Optional[List[types.RestaurantScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Restaurant record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Restaurant filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Restaurant model
        order
            Order the returned Restaurant records by any field
        distinct
            Filter Restaurant records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Restaurant
            The first Restaurant record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Restaurant record ordered by the name field
        restaurant = await Restaurant.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.RestaurantUpdateInput,
        where: types.RestaurantWhereUniqueInput,
        include: Optional[types.RestaurantInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Restaurant record.

        Parameters
        ----------
        data
            Restaurant record data specifying what to update
        where
            Restaurant filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The updated Restaurant record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        restaurant = await Restaurant.prisma().update(
            where={
                'id': 836760821,
            },
            data={
                # data to update the Restaurant record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.RestaurantWhereUniqueInput,
        data: types.RestaurantUpsertInput,
        include: Optional[types.RestaurantInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Restaurant filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Restaurant model

        Returns
        -------
        prisma.models.Restaurant
            The created or updated Restaurant record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        restaurant = await Restaurant.prisma().upsert(
            where={
                'id': 595337866,
            },
            data={
                'create': {
                    'id': 595337866,
                    'name': 'bfaiacjjfc',
                    'location': 'eigcfgbif',
                },
                'update': {
                    'name': 'bfaiacjjfc',
                    'location': 'eigcfgbif',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.RestaurantUpdateManyMutationInput,
        where: types.RestaurantWhereInput,
    ) -> int:
        """Update multiple Restaurant records

        Parameters
        ----------
        data
            Restaurant data to update the selected Restaurant records to
        where
            Filter to select the Restaurant records to update

        Returns
        -------
        int
            The total number of Restaurant records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Restaurant records
        total = await Restaurant.prisma().update_many(
            data={
                'location': 'hjaecfifb'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Restaurant records present in the database

        Parameters
        ----------
        select
            Select the Restaurant fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Restaurant filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RestaurantCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Restaurant.prisma().count()

        # results: prisma.types.RestaurantCountAggregateOutput
        results = await Restaurant.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.RestaurantCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
    ) -> types.RestaurantCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.RestaurantCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RestaurantWhereInput] = None,
        cursor: Optional[types.RestaurantWhereUniqueInput] = None,
    ) -> Union[int, types.RestaurantCountAggregateOutput]:
        """Count the number of Restaurant records present in the database

        Parameters
        ----------
        select
            Select the Restaurant fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Restaurant filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RestaurantCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Restaurant.prisma().count()

        # results: prisma.types.RestaurantCountAggregateOutput
        results = await Restaurant.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RestaurantCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.RestaurantWhereInput] = None
    ) -> int:
        """Delete multiple Restaurant records.

        Parameters
        ----------
        where
            Optional Restaurant filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Restaurant records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Restaurant records
        total = await Restaurant.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.RestaurantScalarFieldKeys'],
        *,
        where: Optional['types.RestaurantWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RestaurantAvgAggregateInput'] = None,
        sum: Optional['types.RestaurantSumAggregateInput'] = None,
        min: Optional['types.RestaurantMinAggregateInput'] = None,
        max: Optional['types.RestaurantMaxAggregateInput'] = None,
        having: Optional['types.RestaurantScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RestaurantCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RestaurantScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RestaurantScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RestaurantGroupByOutput']:
        """Group Restaurant records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Restaurant fields to group records by
        where
            Restaurant filter to select records
        take
            Limit the maximum number of Restaurant records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RestaurantGroupByOutput]
            A list of dictionaries representing the Restaurant record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Restaurant records by location values
        # and count how many records are in each group
        results = await Restaurant.prisma().group_by(
            ['location'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FloorActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Floor]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Floor.prisma().query_raw(
            'SELECT * FROM Floor WHERE id = ?',
            2111915288,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Floor
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Floor.prisma().query_first(
            'SELECT * FROM Floor WHERE name = ?',
            'bbejhfidcb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.FloorCreateInput,
        include: Optional[types.FloorInclude] = None
    ) -> _PrismaModelT:
        """Create a new Floor record.

        Parameters
        ----------
        data
            Floor record data
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The created Floor record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Floor record from just the required fields
        floor = await Floor.prisma().create(
            data={
                # data to create a Floor record
                'name': 'bgeecijdgg',
                'restaurantId': 1388290519,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.FloorCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Floor records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Floor record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Floor.prisma().create_many(
            data=[
                {
                    # data to create a Floor record
                    'name': 'bgehebiafc',
                    'restaurantId': 1675546029,
                },
                {
                    # data to create a Floor record
                    'name': 'bhghchehcc',
                    'restaurantId': 326272115,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.FloorWhereUniqueInput,
        include: Optional[types.FloorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Floor record.

        Parameters
        ----------
        where
            Floor filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The deleted Floor record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        floor = await Floor.prisma().delete(
            where={
                'id': 1343201072,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.FloorWhereUniqueInput,
        include: Optional[types.FloorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Floor record.

        Parameters
        ----------
        where
            Floor filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The found Floor record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        floor = await Floor.prisma().find_unique(
            where={
                'id': 675780521,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.FloorWhereUniqueInput,
        include: Optional[types.FloorInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Floor record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Floor filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The found Floor record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        floor = await Floor.prisma().find_unique_or_raise(
            where={
                'id': 744964398,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
        include: Optional[types.FloorInclude] = None,
        order: Optional[Union[types.FloorOrderByInput, List[types.FloorOrderByInput]]] = None,
        distinct: Optional[List[types.FloorScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Floor records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Floor records returned
        skip
            Ignore the first N results
        where
            Floor filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Floor model
        order
            Order the returned Floor records by any field
        distinct
            Filter Floor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Floor]
            The list of all Floor records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Floor records
        floors = await Floor.prisma().find_many(take=10)

        # find the first 5 Floor records ordered by the restaurantId field
        floors = await Floor.prisma().find_many(
            take=5,
            order={
                'restaurantId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
        include: Optional[types.FloorInclude] = None,
        order: Optional[Union[types.FloorOrderByInput, List[types.FloorOrderByInput]]] = None,
        distinct: Optional[List[types.FloorScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Floor record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Floor filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Floor model
        order
            Order the returned Floor records by any field
        distinct
            Filter Floor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Floor
            The first Floor record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Floor record ordered by the id field
        floor = await Floor.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
        include: Optional[types.FloorInclude] = None,
        order: Optional[Union[types.FloorOrderByInput, List[types.FloorOrderByInput]]] = None,
        distinct: Optional[List[types.FloorScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Floor record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Floor filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Floor model
        order
            Order the returned Floor records by any field
        distinct
            Filter Floor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Floor
            The first Floor record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Floor record ordered by the name field
        floor = await Floor.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.FloorUpdateInput,
        where: types.FloorWhereUniqueInput,
        include: Optional[types.FloorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Floor record.

        Parameters
        ----------
        data
            Floor record data specifying what to update
        where
            Floor filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The updated Floor record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        floor = await Floor.prisma().update(
            where={
                'id': 1969681615,
            },
            data={
                # data to update the Floor record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.FloorWhereUniqueInput,
        data: types.FloorUpsertInput,
        include: Optional[types.FloorInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Floor filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Floor model

        Returns
        -------
        prisma.models.Floor
            The created or updated Floor record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        floor = await Floor.prisma().upsert(
            where={
                'id': 1116175964,
            },
            data={
                'create': {
                    'id': 1116175964,
                    'name': 'bhghchehcc',
                    'restaurantId': 326272115,
                },
                'update': {
                    'name': 'bhghchehcc',
                    'restaurantId': 326272115,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.FloorUpdateManyMutationInput,
        where: types.FloorWhereInput,
    ) -> int:
        """Update multiple Floor records

        Parameters
        ----------
        data
            Floor data to update the selected Floor records to
        where
            Filter to select the Floor records to update

        Returns
        -------
        int
            The total number of Floor records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Floor records
        total = await Floor.prisma().update_many(
            data={
                'restaurantId': 861472101
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Floor records present in the database

        Parameters
        ----------
        select
            Select the Floor fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Floor filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FloorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Floor.prisma().count()

        # results: prisma.types.FloorCountAggregateOutput
        results = await Floor.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.FloorCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
    ) -> types.FloorCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.FloorCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FloorWhereInput] = None,
        cursor: Optional[types.FloorWhereUniqueInput] = None,
    ) -> Union[int, types.FloorCountAggregateOutput]:
        """Count the number of Floor records present in the database

        Parameters
        ----------
        select
            Select the Floor fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Floor filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FloorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Floor.prisma().count()

        # results: prisma.types.FloorCountAggregateOutput
        results = await Floor.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FloorCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.FloorWhereInput] = None
    ) -> int:
        """Delete multiple Floor records.

        Parameters
        ----------
        where
            Optional Floor filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Floor records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Floor records
        total = await Floor.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.FloorScalarFieldKeys'],
        *,
        where: Optional['types.FloorWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FloorAvgAggregateInput'] = None,
        sum: Optional['types.FloorSumAggregateInput'] = None,
        min: Optional['types.FloorMinAggregateInput'] = None,
        max: Optional['types.FloorMaxAggregateInput'] = None,
        having: Optional['types.FloorScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FloorCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FloorScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FloorScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FloorGroupByOutput']:
        """Group Floor records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Floor fields to group records by
        where
            Floor filter to select records
        take
            Limit the maximum number of Floor records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FloorGroupByOutput]
            A list of dictionaries representing the Floor record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Floor records by restaurantId values
        # and count how many records are in each group
        results = await Floor.prisma().group_by(
            ['restaurantId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TableActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Table]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Table.prisma().query_raw(
            'SELECT * FROM Table WHERE id = ?',
            1303003706,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Table
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Table.prisma().query_first(
            'SELECT * FROM Table WHERE number = ?',
            1686638315,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.TableCreateInput,
        include: Optional[types.TableInclude] = None
    ) -> _PrismaModelT:
        """Create a new Table record.

        Parameters
        ----------
        data
            Table record data
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The created Table record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Table record from just the required fields
        table = await Table.prisma().create(
            data={
                # data to create a Table record
                'number': 2000430152,
                'floorId': 1868141281,
                'capacity': 1860847622,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.TableCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Table records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Table record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Table.prisma().create_many(
            data=[
                {
                    # data to create a Table record
                    'number': 1448521415,
                    'floorId': 1628650740,
                    'capacity': 1249606685,
                },
                {
                    # data to create a Table record
                    'number': 835903122,
                    'floorId': 763719779,
                    'capacity': 429995104,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.TableWhereUniqueInput,
        include: Optional[types.TableInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Table record.

        Parameters
        ----------
        where
            Table filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The deleted Table record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        table = await Table.prisma().delete(
            where={
                'id': 1775811865,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.TableWhereUniqueInput,
        include: Optional[types.TableInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Table record.

        Parameters
        ----------
        where
            Table filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The found Table record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        table = await Table.prisma().find_unique(
            where={
                'id': 893145566,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.TableWhereUniqueInput,
        include: Optional[types.TableInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Table record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Table filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The found Table record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        table = await Table.prisma().find_unique_or_raise(
            where={
                'id': 995405759,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
        include: Optional[types.TableInclude] = None,
        order: Optional[Union[types.TableOrderByInput, List[types.TableOrderByInput]]] = None,
        distinct: Optional[List[types.TableScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Table records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Table records returned
        skip
            Ignore the first N results
        where
            Table filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Table model
        order
            Order the returned Table records by any field
        distinct
            Filter Table records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Table]
            The list of all Table records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Table records
        tables = await Table.prisma().find_many(take=10)

        # find the first 5 Table records ordered by the floorId field
        tables = await Table.prisma().find_many(
            take=5,
            order={
                'floorId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
        include: Optional[types.TableInclude] = None,
        order: Optional[Union[types.TableOrderByInput, List[types.TableOrderByInput]]] = None,
        distinct: Optional[List[types.TableScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Table record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Table filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Table model
        order
            Order the returned Table records by any field
        distinct
            Filter Table records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Table
            The first Table record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Table record ordered by the capacity field
        table = await Table.prisma().find_first(
            skip=1,
            order={
                'capacity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
        include: Optional[types.TableInclude] = None,
        order: Optional[Union[types.TableOrderByInput, List[types.TableOrderByInput]]] = None,
        distinct: Optional[List[types.TableScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Table record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Table filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Table model
        order
            Order the returned Table records by any field
        distinct
            Filter Table records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Table
            The first Table record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Table record ordered by the id field
        table = await Table.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.TableUpdateInput,
        where: types.TableWhereUniqueInput,
        include: Optional[types.TableInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Table record.

        Parameters
        ----------
        data
            Table record data specifying what to update
        where
            Table filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The updated Table record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        table = await Table.prisma().update(
            where={
                'id': 2102736524,
            },
            data={
                # data to update the Table record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.TableWhereUniqueInput,
        data: types.TableUpsertInput,
        include: Optional[types.TableInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Table filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Table model

        Returns
        -------
        prisma.models.Table
            The created or updated Table record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        table = await Table.prisma().upsert(
            where={
                'id': 271520213,
            },
            data={
                'create': {
                    'id': 271520213,
                    'number': 835903122,
                    'floorId': 763719779,
                    'capacity': 429995104,
                },
                'update': {
                    'number': 835903122,
                    'floorId': 763719779,
                    'capacity': 429995104,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.TableUpdateManyMutationInput,
        where: types.TableWhereInput,
    ) -> int:
        """Update multiple Table records

        Parameters
        ----------
        data
            Table data to update the selected Table records to
        where
            Filter to select the Table records to update

        Returns
        -------
        int
            The total number of Table records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Table records
        total = await Table.prisma().update_many(
            data={
                'number': 456633834
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Table records present in the database

        Parameters
        ----------
        select
            Select the Table fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Table filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TableCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Table.prisma().count()

        # results: prisma.types.TableCountAggregateOutput
        results = await Table.prisma().count(
            select={
                '_all': True,
                'floorId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.TableCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
    ) -> types.TableCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.TableCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TableWhereInput] = None,
        cursor: Optional[types.TableWhereUniqueInput] = None,
    ) -> Union[int, types.TableCountAggregateOutput]:
        """Count the number of Table records present in the database

        Parameters
        ----------
        select
            Select the Table fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Table filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TableCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Table.prisma().count()

        # results: prisma.types.TableCountAggregateOutput
        results = await Table.prisma().count(
            select={
                '_all': True,
                'capacity': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TableCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.TableWhereInput] = None
    ) -> int:
        """Delete multiple Table records.

        Parameters
        ----------
        where
            Optional Table filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Table records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Table records
        total = await Table.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.TableScalarFieldKeys'],
        *,
        where: Optional['types.TableWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TableAvgAggregateInput'] = None,
        sum: Optional['types.TableSumAggregateInput'] = None,
        min: Optional['types.TableMinAggregateInput'] = None,
        max: Optional['types.TableMaxAggregateInput'] = None,
        having: Optional['types.TableScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TableCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TableScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TableScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TableGroupByOutput']:
        """Group Table records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Table fields to group records by
        where
            Table filter to select records
        take
            Limit the maximum number of Table records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TableGroupByOutput]
            A list of dictionaries representing the Table record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Table records by id values
        # and count how many records are in each group
        results = await Table.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class MemberActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Member]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Member.prisma().query_raw(
            'SELECT * FROM Member WHERE id = ?',
            2058258651,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Member
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Member.prisma().query_first(
            'SELECT * FROM Member WHERE name = ?',
            'bfidgijfjc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.MemberCreateInput,
        include: Optional[types.MemberInclude] = None
    ) -> _PrismaModelT:
        """Create a new Member record.

        Parameters
        ----------
        data
            Member record data
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The created Member record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Member record from just the required fields
        member = await Member.prisma().create(
            data={
                # data to create a Member record
                'name': 'ihieecagf',
                'email': 'bghfciaafe',
                'phone': 'bgchfhgceh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.MemberCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Member records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Member record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Member.prisma().create_many(
            data=[
                {
                    # data to create a Member record
                    'name': 'cafeiaccbc',
                    'email': 'gaddfhfh',
                    'phone': 'gieegcbeg',
                },
                {
                    # data to create a Member record
                    'name': 'bgcffadich',
                    'email': 'fcbichhci',
                    'phone': 'bcggadccgf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.MemberWhereUniqueInput,
        include: Optional[types.MemberInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Member record.

        Parameters
        ----------
        where
            Member filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The deleted Member record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        member = await Member.prisma().delete(
            where={
                'id': 93253262,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.MemberWhereUniqueInput,
        include: Optional[types.MemberInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Member record.

        Parameters
        ----------
        where
            Member filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The found Member record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        member = await Member.prisma().find_unique(
            where={
                'id': 2053047983,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.MemberWhereUniqueInput,
        include: Optional[types.MemberInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Member record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Member filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The found Member record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        member = await Member.prisma().find_unique_or_raise(
            where={
                'id': 685333180,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
        include: Optional[types.MemberInclude] = None,
        order: Optional[Union[types.MemberOrderByInput, List[types.MemberOrderByInput]]] = None,
        distinct: Optional[List[types.MemberScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Member records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Member records returned
        skip
            Ignore the first N results
        where
            Member filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Member model
        order
            Order the returned Member records by any field
        distinct
            Filter Member records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Member]
            The list of all Member records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Member records
        members = await Member.prisma().find_many(take=10)

        # find the first 5 Member records ordered by the email field
        members = await Member.prisma().find_many(
            take=5,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
        include: Optional[types.MemberInclude] = None,
        order: Optional[Union[types.MemberOrderByInput, List[types.MemberOrderByInput]]] = None,
        distinct: Optional[List[types.MemberScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Member record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Member filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Member model
        order
            Order the returned Member records by any field
        distinct
            Filter Member records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Member
            The first Member record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Member record ordered by the phone field
        member = await Member.prisma().find_first(
            skip=1,
            order={
                'phone': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
        include: Optional[types.MemberInclude] = None,
        order: Optional[Union[types.MemberOrderByInput, List[types.MemberOrderByInput]]] = None,
        distinct: Optional[List[types.MemberScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Member record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Member filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Member model
        order
            Order the returned Member records by any field
        distinct
            Filter Member records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Member
            The first Member record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Member record ordered by the id field
        member = await Member.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.MemberUpdateInput,
        where: types.MemberWhereUniqueInput,
        include: Optional[types.MemberInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Member record.

        Parameters
        ----------
        data
            Member record data specifying what to update
        where
            Member filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The updated Member record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        member = await Member.prisma().update(
            where={
                'id': 127474245,
            },
            data={
                # data to update the Member record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.MemberWhereUniqueInput,
        data: types.MemberUpsertInput,
        include: Optional[types.MemberInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Member filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Member model

        Returns
        -------
        prisma.models.Member
            The created or updated Member record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        member = await Member.prisma().upsert(
            where={
                'id': 948921754,
            },
            data={
                'create': {
                    'id': 948921754,
                    'name': 'bgcffadich',
                    'email': 'fcbichhci',
                    'phone': 'bcggadccgf',
                },
                'update': {
                    'name': 'bgcffadich',
                    'email': 'fcbichhci',
                    'phone': 'bcggadccgf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.MemberUpdateManyMutationInput,
        where: types.MemberWhereInput,
    ) -> int:
        """Update multiple Member records

        Parameters
        ----------
        data
            Member data to update the selected Member records to
        where
            Filter to select the Member records to update

        Returns
        -------
        int
            The total number of Member records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Member records
        total = await Member.prisma().update_many(
            data={
                'name': 'bjgejjabff'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Member records present in the database

        Parameters
        ----------
        select
            Select the Member fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Member filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MemberCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Member.prisma().count()

        # results: prisma.types.MemberCountAggregateOutput
        results = await Member.prisma().count(
            select={
                '_all': True,
                'email': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.MemberCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
    ) -> types.MemberCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.MemberCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MemberWhereInput] = None,
        cursor: Optional[types.MemberWhereUniqueInput] = None,
    ) -> Union[int, types.MemberCountAggregateOutput]:
        """Count the number of Member records present in the database

        Parameters
        ----------
        select
            Select the Member fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Member filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MemberCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Member.prisma().count()

        # results: prisma.types.MemberCountAggregateOutput
        results = await Member.prisma().count(
            select={
                '_all': True,
                'phone': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.MemberCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.MemberWhereInput] = None
    ) -> int:
        """Delete multiple Member records.

        Parameters
        ----------
        where
            Optional Member filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Member records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Member records
        total = await Member.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.MemberScalarFieldKeys'],
        *,
        where: Optional['types.MemberWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.MemberAvgAggregateInput'] = None,
        sum: Optional['types.MemberSumAggregateInput'] = None,
        min: Optional['types.MemberMinAggregateInput'] = None,
        max: Optional['types.MemberMaxAggregateInput'] = None,
        having: Optional['types.MemberScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.MemberCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.MemberScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.MemberScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.MemberGroupByOutput']:
        """Group Member records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Member fields to group records by
        where
            Member filter to select records
        take
            Limit the maximum number of Member records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.MemberGroupByOutput]
            A list of dictionaries representing the Member record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Member records by id values
        # and count how many records are in each group
        results = await Member.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReservationActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Reservation]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Reservation.prisma().query_raw(
            'SELECT * FROM Reservation WHERE id = ?',
            1228891816,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Reservation
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Reservation.prisma().query_first(
            'SELECT * FROM Reservation WHERE date = ?',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReservationCreateInput,
        include: Optional[types.ReservationInclude] = None
    ) -> _PrismaModelT:
        """Create a new Reservation record.

        Parameters
        ----------
        data
            Reservation record data
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The created Reservation record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Reservation record from just the required fields
        reservation = await Reservation.prisma().create(
            data={
                # data to create a Reservation record
                'date': datetime.datetime.utcnow(),
                'tableId': 255202753,
                'memberId': 1223573862,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReservationCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Reservation records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Reservation record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Reservation.prisma().create_many(
            data=[
                {
                    # data to create a Reservation record
                    'date': datetime.datetime.utcnow(),
                    'tableId': 541269159,
                    'memberId': 1064846676,
                },
                {
                    # data to create a Reservation record
                    'date': datetime.datetime.utcnow(),
                    'tableId': 508382461,
                    'memberId': 1024265714,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReservationWhereUniqueInput,
        include: Optional[types.ReservationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Reservation record.

        Parameters
        ----------
        where
            Reservation filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The deleted Reservation record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservation = await Reservation.prisma().delete(
            where={
                'id': 872078403,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReservationWhereUniqueInput,
        include: Optional[types.ReservationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Reservation record.

        Parameters
        ----------
        where
            Reservation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The found Reservation record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservation = await Reservation.prisma().find_unique(
            where={
                'id': 1874748096,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReservationWhereUniqueInput,
        include: Optional[types.ReservationInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Reservation record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Reservation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The found Reservation record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservation = await Reservation.prisma().find_unique_or_raise(
            where={
                'id': 916896761,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
        include: Optional[types.ReservationInclude] = None,
        order: Optional[Union[types.ReservationOrderByInput, List[types.ReservationOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Reservation records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Reservation records returned
        skip
            Ignore the first N results
        where
            Reservation filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservation model
        order
            Order the returned Reservation records by any field
        distinct
            Filter Reservation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Reservation]
            The list of all Reservation records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Reservation records
        reservations = await Reservation.prisma().find_many(take=10)

        # find the first 5 Reservation records ordered by the tableId field
        reservations = await Reservation.prisma().find_many(
            take=5,
            order={
                'tableId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
        include: Optional[types.ReservationInclude] = None,
        order: Optional[Union[types.ReservationOrderByInput, List[types.ReservationOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Reservation record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Reservation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservation model
        order
            Order the returned Reservation records by any field
        distinct
            Filter Reservation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Reservation
            The first Reservation record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Reservation record ordered by the memberId field
        reservation = await Reservation.prisma().find_first(
            skip=1,
            order={
                'memberId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
        include: Optional[types.ReservationInclude] = None,
        order: Optional[Union[types.ReservationOrderByInput, List[types.ReservationOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Reservation record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Reservation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservation model
        order
            Order the returned Reservation records by any field
        distinct
            Filter Reservation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Reservation
            The first Reservation record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Reservation record ordered by the id field
        reservation = await Reservation.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReservationUpdateInput,
        where: types.ReservationWhereUniqueInput,
        include: Optional[types.ReservationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Reservation record.

        Parameters
        ----------
        data
            Reservation record data specifying what to update
        where
            Reservation filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The updated Reservation record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        reservation = await Reservation.prisma().update(
            where={
                'id': 769267518,
            },
            data={
                # data to update the Reservation record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReservationWhereUniqueInput,
        data: types.ReservationUpsertInput,
        include: Optional[types.ReservationInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Reservation filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Reservation model

        Returns
        -------
        prisma.models.Reservation
            The created or updated Reservation record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservation = await Reservation.prisma().upsert(
            where={
                'id': 820312479,
            },
            data={
                'create': {
                    'id': 820312479,
                    'date': datetime.datetime.utcnow(),
                    'tableId': 508382461,
                    'memberId': 1024265714,
                },
                'update': {
                    'date': datetime.datetime.utcnow(),
                    'tableId': 508382461,
                    'memberId': 1024265714,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReservationUpdateManyMutationInput,
        where: types.ReservationWhereInput,
    ) -> int:
        """Update multiple Reservation records

        Parameters
        ----------
        data
            Reservation data to update the selected Reservation records to
        where
            Filter to select the Reservation records to update

        Returns
        -------
        int
            The total number of Reservation records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Reservation records
        total = await Reservation.prisma().update_many(
            data={
                'date': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Reservation records present in the database

        Parameters
        ----------
        select
            Select the Reservation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Reservation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Reservation.prisma().count()

        # results: prisma.types.ReservationCountAggregateOutput
        results = await Reservation.prisma().count(
            select={
                '_all': True,
                'tableId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReservationCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
    ) -> types.ReservationCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReservationCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationWhereInput] = None,
        cursor: Optional[types.ReservationWhereUniqueInput] = None,
    ) -> Union[int, types.ReservationCountAggregateOutput]:
        """Count the number of Reservation records present in the database

        Parameters
        ----------
        select
            Select the Reservation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Reservation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Reservation.prisma().count()

        # results: prisma.types.ReservationCountAggregateOutput
        results = await Reservation.prisma().count(
            select={
                '_all': True,
                'memberId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReservationCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReservationWhereInput] = None
    ) -> int:
        """Delete multiple Reservation records.

        Parameters
        ----------
        where
            Optional Reservation filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Reservation records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Reservation records
        total = await Reservation.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReservationScalarFieldKeys'],
        *,
        where: Optional['types.ReservationWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReservationAvgAggregateInput'] = None,
        sum: Optional['types.ReservationSumAggregateInput'] = None,
        min: Optional['types.ReservationMinAggregateInput'] = None,
        max: Optional['types.ReservationMaxAggregateInput'] = None,
        having: Optional['types.ReservationScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReservationCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReservationScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReservationScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReservationGroupByOutput']:
        """Group Reservation records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Reservation fields to group records by
        where
            Reservation filter to select records
        take
            Limit the maximum number of Reservation records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReservationGroupByOutput]
            A list of dictionaries representing the Reservation record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Reservation records by id values
        # and count how many records are in each group
        results = await Reservation.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models